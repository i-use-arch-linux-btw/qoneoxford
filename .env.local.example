# Copy this file to .env.local and fill in the values. See README → Environment variables.
# Localhost only. Use "development" here; staging/production are set on Vercel.
NEXT_PUBLIC_APP_ENV=development

# Supabase — local project for localhost; staging/production use Vercel env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Google OAuth: enable in Supabase Dashboard → Authentication → Providers; no app env vars required.

# Optional: YouTube video IDs for /videos (without these, "Video coming soon" is shown)
# NEXT_PUBLIC_VIDEO_STORY_ID=
# NEXT_PUBLIC_VIDEO_COMMUNITY_ID=

# Optional: override podcast Drive link (defaults to brief URL)
# NEXT_PUBLIC_PODCAST_DRIVE_URL=
