# Production: live build. Set these in Vercel → Project → Settings → Environment Variables (Production).
# Required for auth (login) on deployed site:
# NEXT_PUBLIC_APP_ENV=production
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_APP_ENV=production
